{
  "name": "finance-manager",
  "version": "0.1.0",
  "private": true,
  "description": "Personal Finance Manager - single-user, local-first, PWA-enabled",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "typecheck": "npm run typecheck --workspace=backend && npm run build --workspace=frontend",
    "icons:generate": "npm run icons:generate --workspace=frontend",
    "smoke-test": "npm run smoke-test --workspace=backend",
    "deploy:build": "node scripts/deploy-build.mjs",
    "start:prod": "npm run deploy:build && npm run start --workspace=backend",
    "start": "npm run start --workspace=backend"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
