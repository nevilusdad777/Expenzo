# Development — point at your machine's LAN IP so your phone can reach the API
VITE_API_BASE_URL=http://192.168.1.10:5000

# Production (same-origin deploy) — leave empty to use relative /api paths
# VITE_API_BASE_URL=
